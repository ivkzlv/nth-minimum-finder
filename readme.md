# Инструкция по сборке и запуску приложения NthMinimumFinder

## 🛠 Предварительные требования

Перед началом работы убедитесь, что у вас установлены следующие компоненты:

1. **Java Development Kit (JDK) версии 17 или выше**
    - Проверьте версию: `java -version`
    - Скачать можно с официального сайта: [Oracle JDK](https://www.oracle.com/java/technologies/downloads/) или [OpenJDK](https://adoptium.net/)

2. **Apache Maven 3.6.0 или выше**
    - Проверьте версию: `mvn -v`
    - Скачать можно здесь: [Maven](https://maven.apache.org/download.cgi)

## 📦 Сборка проекта

### 1. Перейдите в корневую директорию проекта

```bash
cd nth-minimum-finder
```

### 2. Проверьте структуру проекта

Убедитесь, что структура проекта соответствует следующей:

```
nth-minimum-finder/
├── pom.xml
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── org/
│   │   │       └── nth/
│   │   │           └── minimum/
│   │   │               └── finder/
│   │   │                   ├── NthMinimumFinderApp.java
│   │   │                   ├── controller/
│   │   │                   │   └── NthMinimumFinderController.java
│   │   │                   ├── dto/
│   │   │                   │   ├── FindRequest.java
│   │   │                   │   └── FindResponse.java
│   │   │                   └── service/
│   │   │                   │   └── NthMinimumFinderService.java
│   │   │                   └── exception/
│   │   │                       ├── Error.java
│   │   │                       └── ErrorHandler.java
│   │   └── resources/
│   │       └── application.yml
```

### 3. Соберите проект с помощью Maven

```bash
mvn clean package
```

Эта команда:
- Очистит предыдущие сборки (`clean`)
- Скомпилирует код
- Запустит тесты (если они есть)
- Создаст JAR-файл в папке `target/`

Если всё прошло успешно, вы увидите сообщение:
```
[INFO] BUILD SUCCESS
```

## ▶️ Запуск приложения

### 1. Запустите приложение

```bash
java -jar target/min-number-finder-1.0.0.jar
```

Приложение запустится на порту 8080 (указано в `application.yml`).

## 🌐 Использование Swagger UI

### 1. Откройте Swagger UI

Перейдите в браузере по адресу: http://localhost:8080/swagger-ui.html

Вы увидите интерфейс Swagger с описанием вашего API.

### 2. Подготовьте тестовый XLSX файл

Создайте XLSX файл с числами в первом столбце, например:

| A   |
|-----|
| 10  |
| 5   |
| 20  |
| 3   |
| 8   |

Сохраните его, например, как `/tmp/test.xlsx` (для Linux/Mac) или `C:\test\test.xlsx` (для Windows).

### 3. Выполните запрос через Swagger

1. В Swagger найдите метод `POST /api/find-nth-min`
2. Нажмите "Try it out"
3. Введите JSON:
   ```json
   {
     "filePath": "{absolute-path}",
     "n": 2
   }
   ```
   (замените путь на актуальный для вашего файла)
4. Нажмите "Execute"

Если всё правильно, вы получите ответ с 2-ым минимальным числом из файла (в примере выше это будет 5).

---
## Возможные улучшения


## 🔒 1. Изменение подхода к работе с файлами

**Решение:**  
Заменить параметр `filePath` на **загрузку файла через MultipartFile**:
```java
@PostMapping("/find-nth-min")
public ResponseEntity<NthMinResponse> findNthMinimum(
    @RequestParam("file") MultipartFile file,
    @RequestParam @Min(1) int n
) { ... }
```

**Почему это соответствует постановке?**  
В формулировке "путь к локальному файлу" подразумевается **локальный путь у клиента**, а не сервера. В веб-контексте это означает загрузку файла.

---

## 📏 2. Валидация формата файла и содержимого

**Проблема:**  
В исходной реализации отсутствовала проверка:
- Является ли файл действительно XLSX
- Содержит ли он только целые числа
- Не пустой ли файл

**Решение:**  
Добавить комплексную валидацию:
```java
// Проверка MIME-типа

// Проверка содержимого при обработке

// Проверка наличия данных
```

## 📈 3. Лимитирование размера файла

**Проблема:**  
Без ограничения размера файла возможен DoS через загрузку очень больших файлов.

**Решение:**  
Добавить ограничение в `application.yml`:
```properties
# Максимальный размер загружаемого файла
spring.servlet.multipart.max-file-size: 10MB
spring.servlet.multipart.max-request-size: 10MB
```

---

## 🧪 4. Юнит-тесты для алгоритма

**Проблема:**  
Отсутствие тестов для критически важного алгоритма поиска.

**Решение:**  
Добавить тесты для всех сценариев.